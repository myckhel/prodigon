# Frontend environment variables (Vite exposes VITE_* prefixed vars to the client)
# For local development, the Vite proxy handles API routing — no config needed.
# For production (Docker), the API is served from the same origin via nginx.
VITE_API_BASE_URL=
